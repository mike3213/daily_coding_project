#include <iostream>
#include <vector>
#include <string>
using namespace std;
int main() {
    int cases = 0;
    cin >> cases;
    while(cases-- > 0) {
        string str;
        cin >> str;
        

        if(cases != 0)
            cout << '\n';
    }

    return 0;
}